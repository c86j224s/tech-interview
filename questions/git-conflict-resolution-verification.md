---
id: "git-conflict-resolution-verification"
title: "merge 충돌을 해결했습니다. 원래 양쪽 변경의 의도가 모두 남았는지 어떤 diff와 테스트로 확인하나요?"
difficulty: "중하"
category: "설계"
tags: ["Git","merge","rebase","이력","심화 질문"]
related: ["git-merge-rebase"]
promotedFrom: {"id":"git-merge-rebase","prompt":"merge conflict를 해결한 뒤 원래 변경과 상대 변경이 모두 보존됐는지 어떤 diff로 검증할까요?"}
---

# merge 충돌을 해결했습니다. 원래 양쪽 변경의 의도가 모두 남았는지 어떤 diff와 테스트로 확인하나요?

## 구두 답변

merge 결과와 양 부모의 변경을 비교하고 충돌 해결에서 어떤 의도를 선택했는지 기록합니다. 충돌 표시가 사라졌다는 사실은 두 기능이 모두 보존됐다는 증거가 아닙니다.

통합 후 공통 타입·호출자·테스트를 다시 실행합니다. Git이 자동 병합한 곳도 의미 충돌이 있을 수 있습니다. 원래 변경을 임시 참조로 보존하고 불필요한 포맷·생성 파일 차이를 분리해 검토합니다.

## 득점 포인트

- merge 결과와 양 부모의 변경을 비교하고 충돌 해결에서 어떤 의도를 선택했는지 기록합니다. 충돌 표시가 사라졌다는 사실은 두 기능이 모두 보존됐다는 증거가 아닙니다.
- 원래 변경을 임시 참조로 보존하고 불필요한 포맷·생성 파일 차이를 분리해 검토합니다.

## 감점 포인트

- 다음 핵심 구분을 반대로 설명하거나 성립 조건을 생략한다: merge 결과와 양 부모의 변경을 비교하고 충돌 해결에서 어떤 의도를 선택했는지 기록합니다.

## 더 파고들 거리

- [기본 상황과 비교: 두 브랜치의 변경을 합칠 때 merge와 rebase는 이력을 어떻게 다르게 만들며, 공유 브랜치에서는 무엇을 조심해야 하나요?](/tech-interview/questions/git-merge-rebase/)
