---
id: "idempotency-record-expiry-contract"
title: "멱등 기록을 삭제한 뒤 오래된 재시도가 왔습니다. 새로운 요청으로 실행할지 거절할지 어떤 유효기간 계약을 두나요?"
difficulty: "중하"
category: "네트워크"
tags: ["HTTP","GET","POST","안전성","캐시","심화 질문"]
related: ["http-get-post-semantics","http-cache-validation","csrf-vs-xss"]
promotedFrom: {"id":"http-get-post-semantics","prompt":"idempotency key가 만료된 뒤 늦은 재시도가 도착하는 경우를 어떻게 처리할까요?"}
---

# 멱등 기록을 삭제한 뒤 오래된 재시도가 왔습니다. 새로운 요청으로 실행할지 거절할지 어떤 유효기간 계약을 두나요?

## 구두 답변

멱등 키 보존 기간 밖의 요청은 원래 결과를 보장할 수 없으므로 API에 재시도 유효기간을 명시합니다. 오래된 키를 새 작업으로 무조건 수용하면 중복 효과가 생깁니다.

업무 원장의 안정 ID로 조회하거나 만료 요청을 거절·재확인합니다. 같은 키의 다른 payload와 사용자 범위를 검사하고 기록 삭제·재개·외부 응답 유실을 시험합니다.

## 득점 포인트

- 멱등 키 보존 기간 밖의 요청은 원래 결과를 보장할 수 없으므로 API에 재시도 유효기간을 명시합니다. 오래된 키를 새 작업으로 무조건 수용하면 중복 효과가 생깁니다.
- 같은 키의 다른 payload와 사용자 범위를 검사하고 기록 삭제·재개·외부 응답 유실을 시험합니다.

## 감점 포인트

- 다음 핵심 구분을 반대로 설명하거나 성립 조건을 생략한다: 멱등 키 보존 기간 밖의 요청은 원래 결과를 보장할 수 없으므로 API에 재시도 유효기간을 명시합니다.

## 더 파고들 거리

- [기본 상황과 비교: 검색과 등록 API의 HTTP 메서드를 정하려 합니다. GET과 POST의 의미는 어떻게 다르며, 민감한 값을 POST 본문에 넣는 것만으로 보호할 수 있나요?](/tech-interview/questions/http-get-post-semantics/)
