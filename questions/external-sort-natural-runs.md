---
id: "external-sort-natural-runs"
title: "입력에 이미 정렬된 구간이 많습니다. natural run 탐지로 디스크 쓰기를 줄일 때 어떤 안정성·최악 비용이 남나요?"
difficulty: "중하"
category: "알고리즘"
tags: ["병합 정렬","외부 정렬","I/O","심화 질문"]
related: ["merge-sort-external"]
promotedFrom: {"id":"merge-sort-external","prompt":"이미 정렬된 구간을 활용해 run 수와 디스크 쓰기를 줄이는 방법을 말해 보세요."}
---

# 입력에 이미 정렬된 구간이 많습니다. natural run 탐지로 디스크 쓰기를 줄일 때 어떤 안정성·최악 비용이 남나요?

## 구두 답변

입력의 이미 정렬된 run을 감지해 그대로 병합하면 초기 정렬·run 수·디스크 pass를 줄일 수 있습니다. 하지만 무작위·교차 패턴에서는 감지 비용과 작은 run 관리가 남습니다.

내림차순 run 반전이 동점 안정성을 깨지 않는지 확인합니다. 원본 순번이나 안정 병합 기준을 유지하고 큰 레코드·빈 run·중단 파일을 시험합니다. 전체 I/O와 임시 공간으로 효과를 평가합니다.

## 득점 포인트

- 입력의 이미 정렬된 run을 감지해 그대로 병합하면 초기 정렬·run 수·디스크 pass를 줄일 수 있습니다. 하지만 무작위·교차 패턴에서는 감지 비용과 작은 run 관리가 남습니다.
- 전체 I/O와 임시 공간으로 효과를 평가합니다.

## 감점 포인트

- 다음 핵심 구분을 반대로 설명하거나 성립 조건을 생략한다: 입력의 이미 정렬된 run을 감지해 그대로 병합하면 초기 정렬·run 수·디스크 pass를 줄일 수 있습니다.

## 더 파고들 거리

- [기본 상황과 비교: 메모리에 다 들어가지 않는 큰 파일을 정렬하려 합니다. 병합 정렬은 어떻게 활용할 수 있나요?](/tech-interview/questions/merge-sort-external/)
