---
id: python-mutable-default
title: "Python 함수의 기본 인자를 빈 리스트로 뒀는데 이전 호출에서 추가한 값이 다음 호출에도 남습니다. 기본값 객체는 언제 만들어지며 어떻게 고치나요?"
difficulty: 하
category: 언어·런타임
tags: ["Python","기본 인자","mutable","리스트"]
related: []
---

# Python 함수의 기본 인자를 빈 리스트로 뒀는데 이전 호출에서 추가한 값이 다음 호출에도 남습니다. 기본값 객체는 언제 만들어지며 어떻게 고치나요?

## 구두 답변

함수의 기본 인자는 함수가 호출될 때마다 만드는 값이 아니라 함수 정의를 평가할 때 한 번 만들어집니다. 따라서 기본값으로 리스트나 딕셔너리 같은 mutable 객체를 두고 함수 안에서 수정하면, 그 객체가 다음 호출에도 그대로 남습니다. 이는 일반적인 메모리 누수라기보다 함수 호출 사이의 상태 공유를 의도하지 않게 만든 버그입니다.

```python
def add_item(item, bucket=[]):
    bucket.append(item)
    return bucket

print(add_item("a"))
print(add_item("b"))
```

출력은 `['a']` 다음 `['a', 'b']`입니다. 두 호출이 서로 다른 빈 리스트를 받은 것이 아니라 같은 기본 리스트를 사용했기 때문입니다. 기본값으로 None을 두고, 인자가 생략된 호출마다 본문에서 새 리스트를 만들면 의도하지 않은 가변 상태 공유를 피할 수 있습니다. 불변 기본값도 재사용될 수 있지만 그 객체 자체를 변경하는 문제가 없다는 차이가 있습니다.

```python
def add_item(item, bucket=None):
    if bucket is None:
        bucket = []
    bucket.append(item)
    return bucket

print(add_item("a"))
print(add_item("b"))
```

이 경우 출력은 각각 `['a']`, `['b']`입니다. 단, 호출자가 같은 리스트를 명시적으로 넘겨 여러 호출에서 누적하려는 API라면 공유가 의도일 수 있으므로 그 계약을 문서화하겠습니다. 기본 인자에서 `[]`를 피하는 것만으로 모든 공유 상태가 사라지는 것은 아니며, 함수 본문이 전역·캐시·클래스 속성을 수정하는지도 함께 확인해야 합니다.

## 득점 포인트

- 기본 인자가 정의 시 한 번 평가된다는 시점을 설명한다.
- 호출 간 동일 리스트가 변형되는 실제 출력과 `None` sentinel 수정법을 제시한다.
- 명시적으로 전달한 mutable 객체의 공유와 의도하지 않은 기본값 공유를 구분한다.

## 감점 포인트

- 기본 인자는 호출 때마다 새 객체로 만들어진다고 말한다.
- `None`을 리스트처럼 바로 `append`할 수 있다고 설명한다.
- 이 현상을 모든 경우의 메모리 누수라고 부르며 상태 공유 문제를 설명하지 않는다.

## 더 파고들 거리

- `dict`와 `set` 기본값에서도 같은 현상이 어떻게 나타나는지 비교해 보세요.
- 기본 인자 표현식의 평가 시점과 클로저의 late binding은 어떻게 다른가요?
- 기본값 객체를 의도적으로 캐시로 사용할 때 어떤 동시성 계약이 필요할까요?
