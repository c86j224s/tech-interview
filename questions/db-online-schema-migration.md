---
id: db-online-schema-migration
title: "운영 중인 DB의 문자열 상태를 새 코드 컬럼으로 옮기려 합니다. 구버전 서버가 남아 있고 쓰기가 계속될 때 어떻게 전환하나요?"
difficulty: 하
category: 데이터베이스
tags:
  - "스키마 마이그레이션"
  - "호환성"
  - "배포"
related: ["db-lock-escalation"]
---

# 운영 중인 DB의 문자열 상태를 새 코드 컬럼으로 옮기려 합니다. 구버전 서버가 남아 있고 쓰기가 계속될 때 어떻게 전환하나요?

## 구두 답변

애플리케이션과 DB를 한 번에 바꾸기보다 구버전과 신버전이 함께 동작하는 기간을 먼저 설계하겠습니다. 새 컬럼을 추가하는 단계, 데이터를 채우는 단계, 읽기·쓰기를 전환하는 단계, 이전 컬럼을 제거하는 단계를 나누는 방식입니다.

기존 status="paid"를 새 status_code=2로 옮긴다고 하겠습니다. 과거 행을 채우는 작업을 백필이라고 합니다. 백필이 paid를 읽은 사이 실제 상태가 cancelled로 바뀌었다면, 나중에 무조건 2를 쓰면 안 됩니다. 같은 UPDATE에서 현재 문자열로 코드를 계산하거나, 읽은 버전이 그대로일 때만 쓰고 충돌한 행은 다시 처리하겠습니다.

문자열 상태를 새 코드 컬럼으로 옮긴다면 먼저 기존 코드가 무시해도 되는 컬럼을 추가하겠습니다. 새 버전은 두 형태를 호환하고, 과거 데이터는 작은 배치로 채웁니다. 이때 백필 중 새 쓰기가 발생하므로 단순히 오래된 스냅샷 값을 덮어쓰지 않게 조건과 버전을 확인해야 합니다. 새 컬럼의 누락과 값 차이가 없는지 검증한 뒤 읽기를 전환하겠습니다.

DB의 온라인 DDL 지원도 이름만 믿지 않겠습니다. 시작·마무리 시 잠금이나 로그·복제 부하가 생길 수 있어 실제 데이터 크기에서 확인해야 합니다. 롤백이 필요하면 이전 코드가 읽을 데이터가 계속 유지돼야 하므로, 전환 직후 이전 컬럼을 지우지 않겠습니다. 변경의 성공 기준은 배포 완료가 아니라 혼합 버전에서 오류가 없고, 백필이 재시작 가능하며, 제거 전에 더 이상 이전 형식을 쓰는 경로가 없다는 것입니다.

## 득점 포인트

- 추가·호환·백필·전환·제거를 분리한다.
- 백필과 동시 쓰기의 낡은 값 덮어쓰기를 막는다.
- DDL 비용과 롤백·구버전 소비자를 검증한다.

## 감점 포인트

- 코드와 컬럼을 한 번에 교체한다.
- 온라인 DDL은 잠금이 전혀 없다고 말한다.
- 읽기 전환 직후 이전 컬럼을 삭제한다.

## 더 파고들 거리

- 대규모 백필의 진행 기준을 어떻게 저장할까요?
- 두 컬럼 불일치 시 어느 이력을 기준으로 복구할까요?
- NOT NULL·기본값 추가 비용을 어떻게 검증할까요?
