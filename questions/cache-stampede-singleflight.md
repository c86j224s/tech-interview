---
id: cache-stampede-singleflight
title: "인기 캐시가 만료될 때 DB로 요청이 몰리는 현상은 어떻게 줄이나요?"
category: 분산 시스템
tags:
  - "캐시"
  - "캐시 스탬피드"
  - "singleflight"
related: ["cache-aside-consistency"]
---

# 인기 캐시가 만료될 때 DB로 요청이 몰리는 현상은 어떻게 줄이나요?

## 구두 답변

동시에 같은 키를 놓친 요청들이 모두 DB를 읽는 것이 문제입니다. 캐시가 있을 때는 감춰졌던 부하가 만료 순간에 한꺼번에 드러납니다. 저는 먼저 같은 키에 대한 채우기 작업을 합칠 수 있는지 보겠습니다.

프로세스 안에서는 singleflight처럼 첫 요청 하나가 데이터를 읽고 나머지가 그 결과를 기다리게 할 수 있습니다. 다만 여러 인스턴스가 있으면 인스턴스별 한 번씩은 여전히 DB를 읽습니다. 그 양도 감당하기 어렵다면 분산 lease나 선제 갱신을 검토하겠습니다. 이때 채우기 작업자가 죽으면 대기자가 영원히 멈추지 않도록 제한 시간과 재시도 소유권이 필요합니다.

조금 오래된 값을 허용하는 데이터라면 기존 값을 제공하면서 뒤에서 갱신하는 방식이 지연을 줄일 수 있습니다. 반면 권한 같은 데이터는 오래된 값 제공이 위험할 수 있어 동일 정책을 적용하지 않겠습니다. 여러 키가 같은 시각에 만료되지 않게 TTL에 무작위 차이를 주는 것도 도움이 되지만 단일 핫키 문제를 혼자 해결하지는 못합니다. 테스트는 만료 순간과 갱신 실패를 재현해 origin 호출 수, 대기 시간, 오래된 응답 비율이 계약 안에 있는지 확인하겠습니다.

## 득점 포인트

- **동일 키의 중복 채우기를 묶는다.** 프로세스와 분산 범위를 구분한다.
- **작업자 실패를 다룬다.** lease·대기 제한·재시도 책임을 정한다.
- **오래된 값의 허용 여부를 나눈다.** 데이터 성격에 맞는 정책을 선택한다.

## 감점 포인트

- **TTL jitter만으로 핫키가 해결된다고 말한다.** 단일 키 동시 요청은 남는다.
- **singleflight가 모든 인스턴스를 합친다고 말한다.** 구현 범위를 확인해야 한다.
- **갱신 실패 시 stale 값을 무제한 제공한다.** 최신성 계약을 위반할 수 있다.

## 더 파고들 거리

- refresh-ahead가 실제로 필요 없는 키까지 읽는 비용은 어떻게 제한할까요?
- 채우기 작업의 취소를 첫 호출자의 수명에 묶으면 어떤 문제가 생길까요?
- 캐시 장애와 단일 만료를 구분해 origin 보호 정책을 어떻게 바꿀까요?
