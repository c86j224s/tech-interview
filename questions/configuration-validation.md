---
id: configuration-validation
title: "DB 연결 풀을 인스턴스마다 20개에서 100개로 늘리려 합니다. 값 자체는 유효한데 어떤 장애가 생길 수 있고 어떻게 안전하게 적용하나요?"
difficulty: 하
category: 설계
tags:
  - "설정"
  - "검증"
  - "배포"
related: ["feature-flag-rollout"]
---

# DB 연결 풀을 인스턴스마다 20개에서 100개로 늘리려 합니다. 값 자체는 유효한데 어떤 장애가 생길 수 있고 어떻게 안전하게 적용하나요?

## 구두 답변

설정도 실행 동작을 바꾸므로 코드와 비슷한 검증과 추적이 필요합니다. 형식이 맞는 것과 운영상 안전한 것은 다릅니다. 예를 들어 연결 풀 크기가 숫자로 유효해도 전체 인스턴스 합계가 DB 한도를 넘으면 장애가 날 수 있습니다.

예를 들어 인스턴스가 10개면 설정상 최대 연결은 200개에서 1,000개로 바뀝니다. 이것은 실제로 즉시 1,000개를 연다는 뜻은 아니지만, 부하가 늘 때 DB가 감당해야 할 상한이 달라진다는 뜻입니다. 다른 서비스가 쓰는 연결을 제외한 예산보다 크다면 설정을 거절하거나 전체 동시성을 제한하겠습니다.

저는 설정 스키마로 타입·범위·필수값을 확인하고, 서로 의존하는 값은 함께 검증하겠습니다. 요청 타임아웃보다 내부 재시도 시간이 길거나 캐시 한도가 메모리 예산을 넘는 조합도 거절해야 합니다. 시작 시 잘못된 설정이면 조용히 기본값으로 실행하기보다 명확히 실패하는 편이 안전한 경우가 많습니다. 동적 설정은 마지막 정상 버전 유지와 변경 실패 알림을 둘 수 있습니다.

변경은 작성자·버전·적용 시점을 남기고 일부 인스턴스에서 먼저 확인하겠습니다. 모든 인스턴스가 같은 순간에 바뀐다고 가정하지 않아야 합니다. 설정 저장소 장애 때 새 값을 못 받는 것과 기존 정상 동작을 계속할 수 없는 것은 구분하겠습니다. 테스트에서는 잘못된 단일 값뿐 아니라 유효하지만 위험한 조합과 전파 지연을 재현합니다. 비밀값은 검증 로그에도 원문을 남기지 않겠습니다.

## 득점 포인트

- 인스턴스별 설정과 전체 연결 상한을 숫자 사례로 비교하고 값 사이의 제약을 검증한다.
- 시작 실패와 마지막 정상값 유지의 조건을 나눈다.
- 혼합 버전과 부분 적용을 포함해 변경을 추적한다.

## 감점 포인트

- JSON 파싱 성공만으로 안전하다고 판단한다.
- 모든 오류에서 기본값을 사용해 위험을 숨긴다.
- 전체 설정이나 비밀값을 로그에 남긴다.

## 더 파고들 거리

- 여러 설정을 원자적으로 바꾸지 못할 때 중간 상태를 어떻게 제한할까요?
- 동적으로 바꿀 수 없는 자원 설정을 재시작에 어떻게 반영할까요?
- 설정 변경과 오류율을 어떤 변경 버전으로 연결할까요?
