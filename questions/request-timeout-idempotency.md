---
id: request-timeout-idempotency
title: "요청이 타임아웃됐을 때 그대로 재시도해도 되나요?"
difficulty: 하
category: 분산 시스템
tags:
  - 타임아웃
  - 재시도
  - 멱등성
  - 멱등성 키
  - outbox
related:
  - tcp-stream-message-framing
  - transaction-and-lost-update
---

# 요청이 타임아웃됐을 때 그대로 재시도해도 되나요?

## 구두 답변

그대로 재시도해도 되는지는 작업의 성격에 달려 있습니다. 타임아웃은 제가 결과를 못 받았다는 뜻이지 서버가 실행하지 않았다는 뜻은 아닙니다. 주문은 저장됐는데 응답만 유실됐을 수도 있습니다. 그래서 저는 **실패했는지 모르는 상태에서 다시 보내도 같은 업무가 두 번 반영되지 않는지**부터 확인하겠습니다.

이때 필요한 성질이 멱등성입니다. 같은 논리적 요청을 반복해도 상태 변화는 한 번 실행한 것과 같게 만드는 것입니다. 잔액 차감처럼 반복하면 결과가 달라지는 API에는 클라이언트가 요청 키를 만들고, 재시도에도 같은 키를 보내게 하겠습니다. 서버는 이미 완료한 키라면 기존 결과를 돌려주고, 같은 키로 다른 내용이 오면 잘못된 재사용으로 거절해야 합니다.

중요한 것은 키를 조회한 다음 업무를 수행하는 틈입니다. 두 요청이 동시에 키가 없다고 읽으면 둘 다 실행할 수 있습니다. 같은 DB 안에서 끝나는 작업이라면 키의 고유 제약으로 중복을 막고, 키 기록과 업무 변경, 결과 저장을 하나의 트랜잭션으로 묶겠습니다. 그러면 커밋 전 실패는 함께 취소되고, 커밋 뒤 응답을 잃어도 저장된 결과로 재시도에 답할 수 있습니다.

외부 결제까지 포함하면 이 방법만으로는 부족합니다. 외부 결제가 성공했는데 로컬 결과 저장 전에 죽을 수 있기 때문입니다. 저는 상대가 멱등성 키를 지원하면 같은 작업 키를 넘기고, 결과가 불확실하면 상태를 조회해 확인하겠습니다. 지원하지 않는다면 무조건 다시 결제하기보다 확인이 필요한 상태로 남겨 대사하는 절차가 필요합니다. 여기서는 빠른 자동 복구보다 중복 결제를 막는 쪽을 우선하겠습니다.

재시도 자체가 장애를 키우지 않게 전체 시간과 횟수도 제한하겠습니다. 잠깐의 과부하라면 간격을 늘리고 약간씩 무작위로 분산해 다시 보내지만, 여러 계층에서 각각 재시도하지는 않도록 책임을 정해야 합니다. 테스트는 특히 **커밋 직후 응답이 끊긴 경우와 같은 키가 동시에 들어온 경우**를 재현하겠습니다. 정상 요청이 한 번 성공하는 것보다 불확실한 실패 뒤에도 효과가 한 번만 남는지를 확인하는 것이 핵심입니다.

## 득점 포인트

- **타임아웃의 불확실성을 사례로 설명한다.** 서버 커밋과 응답 전달을 분리해서 생각한다.
- **중복 방지의 원자적 범위를 정한다.** 고유 제약과 키·업무·결과의 동일 트랜잭션으로 경쟁과 크래시 구간을 다룬다.
- **외부 효과에서 보장의 한계를 인정한다.** 외부 멱등성·상태 확인·대사 중 가능한 수단으로 중복 위험을 통제한다.
- **재시도 제한과 장애 주입을 구체화한다.** 횟수·시간·책임 계층을 정하고 응답 유실과 동시 중복을 테스트한다.

## 감점 포인트

- **“타임아웃이면 서버도 실패했다”고 말한다.** 작업은 커밋됐고 응답만 유실됐을 수 있다.
- **“키 존재 여부를 먼저 조회하면 충분하다”고 말한다.** 조회와 실행 사이에 다른 요청이 통과할 수 있다.
- **“재시도마다 새 키를 사용한다”고 말한다.** 같은 논리 작업의 반복임을 식별할 수 없어진다.
- **“외부 호출도 로컬 트랜잭션이 롤백해 준다”고 말한다.** 외부 성공은 별도의 상태 확인과 복구가 필요하다.

## 더 파고들 거리

- 처리 중 상태를 먼저 저장한 워커가 죽으면 요청 소유권과 재처리를 어떻게 안전하게 넘길까요?
- outbox는 어떤 유실을 막으며, 메시지를 받는 쪽의 중복 방지가 여전히 필요한 이유는 무엇일까요?
- 멱등성 기록을 삭제한 뒤 늦게 온 재시도에 대해서는 어떤 유효 기간 계약을 둘까요?
- 여러 계층의 재시도가 겹치지 않게 데드라인과 재시도 예산을 어떻게 전파할까요?
