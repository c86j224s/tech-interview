---
id: "distributed-topk-merge-complexity"
title: "샤드 S개의 정렬된 상위 K 후보를 병합합니다. heap·네트워크·메모리 비용은 어떻게 계산하나요?"
difficulty: "중하"
category: "자료구조"
tags: ["랭킹","top-K","샤딩","심화 질문"]
related: ["ranking-global-topk","ranking-top-k"]
promotedFrom: {"id":"ranking-global-topk","prompt":"S개 샤드의 top-K 후보를 힙으로 병합할 때 시간·메모리·네트워크 비용을 계산해 보세요."}
---

# 샤드 S개의 정렬된 상위 K 후보를 병합합니다. heap·네트워크·메모리 비용은 어떻게 계산하나요?

## 구두 답변

각 샤드의 후보가 같은 전체 비교 순서를 쓰고 사용자 점수가 샤드에 완전히 속한다는 전제가 필요합니다. 최대 S×K 후보를 크기 K heap으로 처리하거나 정렬 목록을 S-way 병합할 수 있습니다.

K-way 출력의 heap 비용·전송·buffer를 구분합니다. 동점자 전원 요구나 분할 점수는 후보 상한을 바꿉니다. 같은 snapshot·샤드 실패·중복 사용자를 포함해 전체 정렬 기준과 비교합니다.

## 득점 포인트

- 각 샤드의 후보가 같은 전체 비교 순서를 쓰고 사용자 점수가 샤드에 완전히 속한다는 전제가 필요합니다. 최대 S×K 후보를 크기 K heap으로 처리하거나 정렬 목록을 S-way 병합할 수 있습니다.
- 같은 snapshot·샤드 실패·중복 사용자를 포함해 전체 정렬 기준과 비교합니다.

## 감점 포인트

- 다음 핵심 구분을 반대로 설명하거나 성립 조건을 생략한다: 각 샤드의 후보가 같은 전체 비교 순서를 쓰고 사용자 점수가 샤드에 완전히 속한다는 전제가 필요합니다.

## 더 파고들 거리

- [기본 상황과 비교: 사용자별 점수가 샤드에 완전히 분할된 경우 샤드별 상위 K명만으로 전역 상위 K명을 구할 수 있나요?](/tech-interview/questions/ranking-global-topk/)
