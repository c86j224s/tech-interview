---
id: agent-trial-compute
title: 반복 신뢰도와 추가 추론의 한계 효용
topic: AI 에이전트
summary: pass@k·모든 시도 성공·과제별 집계·상관 오류와 선택기를 구분하고 후보 탐색·평가 수정·회귀 보존·동일 예산 비교를 설명합니다.
questionIds: [agent-reliability-trials, agent-compute-budget-scaling, agent-evaluator-optimizer, agent-multiagent-correlated-errors]
---

# 반복 신뢰도와 추가 추론의 한계 효용

## 한 번이라도 성공하는 것과 매번 성공하는 것은 다릅니다

독립·동일 성공확률 p의 k회에서 적어도 한 번 성공은 `1-(1-p)^k`, 모두 성공은 `p^k`입니다. p=0.75, k=3이면 각각 0.984375와 0.421875입니다. 후보 탐색의 pass@k가 높아도 한 번 처리하는 운영 요청이 98.4% 성공한다는 뜻은 아닙니다. 모든 시도 성공은 pass^k라는 표기로 사용되기도 하므로 보고서에서 정의를 명시합니다.

유한 표본 n개 중 c개가 성공했다면 k≤n에서 표본 기반 pass@k 추정에 `1-C(n-c,k)/C(n,k)`를 사용하기도 합니다. 이것도 평가의 sampling·선택 가정을 적어야 하며 피드백을 받아 고치는 연속 실행과 같은 실험은 아닙니다.

## 평균을 먼저 구하면 과제별 특성을 잃을 수 있습니다

쉬운 과제 p=1, 어려운 과제 p=0을 반씩 섞으면 평균 p=0.5입니다. 평균에 제곱하면 0.25지만 과제별 두 번 모두 성공 확률의 평균은 0.5입니다. 과제별 반복 결과·예산·분산·실패 유형을 저장하고 어느 집계를 썼는지 밝힙니다. 작은 표본에서 과도한 소수점 정밀도를 주장하지 않습니다.

| 지표 | 답하는 질문 | 빠지기 쉬운 조건 |
| --- | --- | --- |
| 단일 trial 성공 | 한번 처리하면 해결하는가 | 과제 난도 혼합 |
| pass@k | k개 중 성공 후보가 있는가 | 실제로 찾을 선택기 필요 |
| 모두 성공 | 반복 서비스가 안정적인가 | 공동 실패·비독립 |
| 선택 후 성공 | 사용자가 받는 후보가 맞는가 | 검증기 오탐·비용 |
| 성공당 비용 | 실패까지 합쳐 얼마인가 | 사람 검토·도구·재시도 |

## 세 모델의 합의도 같은 오해일 수 있습니다

같은 오래된 문서를 읽으면 모델·역할 이름이 달라도 모두 틀릴 수 있습니다. 독립 탐색에는 다른 결론을 먼저 주어 따라가게 하지 않고 규범 원문·SDK·실제 재현처럼 증거 방법을 나눕니다. 반대로 특정 결함의 반박 검증에는 원래 주장을 주는 것이 목적에 맞습니다.

compilation·중복 예약 같은 확인 가능한 사실은 투표 대신 실제 검사로 판단합니다. 공동 실패율·추가로 찾은 결함·새 오탐·올바른 소수 의견을 잘못 덮은 비율을 봅니다. 모델 합의는 사용자 승인이나 데이터 전송 권한도 아닙니다.

```diagram
{"title":"추가 계산의 가치는 검증 후 결과로 판단합니다","caption":"화살표는 후보 생성과 선택입니다. 모든 후보의 비용을 세고 실제 외부 변경은 검증·승인된 한 실행 경로로 분리합니다.","rows":[[{"id":"task","label":"동일 과제·공유 예산"}],[{"id":"candidate","label":"추론 확대·후보 생성"},{"id":"evidence","label":"새 원문·실행 검증"}],[{"id":"select","label":"고정 기준으로 후보 검사"}],[{"id":"result","label":"검증된 결과·비용·잔여 불확실성"}]],"edges":[{"from":"task","to":"candidate","label":"계산 배분"},{"from":"task","to":"evidence","label":"근거 배분"},{"from":"candidate","to":"select","label":"격리된 후보"},{"from":"evidence","to":"select","label":"독립 검사"},{"from":"select","to":"result","label":"실제 선택 품질"}]}
```

## 비평은 고칠 수 있는 결함을 돌려줘야 합니다

“더 자세히”는 길이만 늘릴 수 있습니다. “선택 기능을 필수로 바꿨다”, “분모가 전체가 아니라 성공 요청이다”처럼 원문·조건·수정 지점을 반환합니다. 비평자가 원문이나 실행 결과 없이 생성 답만 읽으면 같은 오해를 반복할 수 있습니다. 미확인·문제 없음도 허용합니다.

수정 전후 version·diff·핵심 불변식·회귀 test를 보존합니다. 검증된 부분까지 통째로 다시 쓰게 하지 않고 고칠 항목과 유지할 항목을 지정합니다. 마지막 생성물보다 검증된 최선 결과를 남기며 생성기가 채점 기준을 약화하지 못하게 합니다.

## 더 오래 생각하는 대신 새 사실이 필요할 수 있습니다

추론 예산·독립 후보·원문 조회·테스트·평가 수정은 서로 다른 자원 배분입니다. 열 후보가 같은 잘못된 수치를 반복할 때 작은 계산 스크립트 하나가 더 가치 있을 수 있습니다. 후보 생성은 외부 효과 없는 격리 환경에서 하고 각 후보가 실제 결제를 시도하게 만들지 않습니다.

예산을 단계적으로 늘려 유형별 성공·안전·지연·성공당 비용 곡선을 비교합니다. 개선 없음·같은 실패·상한 도달이면 새 근거·사람 검토·보류를 선택합니다. 이 노트의 확률은 명시된 수학 가정의 예시이며 실제 에이전트 성공률이나 독립성을 측정한 값은 아닙니다.
