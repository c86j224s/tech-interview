---
id: scene-persistence
title: 멀티 씬 문서의 저장과 충돌 복구
topic: 모바일
summary: 종료 콜백에 의존하지 않는 저장, 씬별 UI와 공용 문서 소유, 중복 저장·역순 완료·동시 편집의 버전 계약을 설명합니다.
questionIds: [ios-scene-app-lifecycle, ios-scene-save-version-dedup, ios-multiscene-document-conflict]
---

# 멀티 씬 문서의 저장과 충돌 복구

## 마지막 종료 알림이 오지 않을 수 있습니다

사용자가 문서를 편집한 뒤 앱이 종료됐을 때 마지막 콜백에서 저장하겠다는 설계는 안전하지 않습니다. 시스템 종료·강제 종료 상황에서 마지막 알림이 항상 호출된다고 가정할 수 없습니다. 중요한 변경은 더 이른 시점에 저장하고 백그라운드 진입은 추가 저장 기회로 다룹니다.

멀티 씬에서는 한 프로세스 안의 여러 창이 서로 다른 활성 상태를 가질 수 있습니다. 한 씬이 배경으로 갔다고 모든 씬이 같이 멈추는 것도 아닙니다. 씬별 화면 복원 상태와 여러 씬이 공유하는 문서 상태의 소유자를 나누어야 합니다.

## 씬별 상태와 문서의 권위를 분리합니다

| 상태 | 대표 소유자 | 예 |
| --- | --- | --- |
| 씬 UI | 씬별 저장 | 선택 문서·스크롤·패널 위치 |
| 문서 본문 | 공용 문서 저장소 | 텍스트·내용 버전 |
| 계정 자격 | 적절한 자격 저장소 | 로그인 세션·토큰 |
| 동기화 진행 | 공용 작업 상태 | 업로드 위치·불확정 결과 |

```diagram
{"title":"두 씬은 하나의 문서 저장소를 공유합니다","caption":"화살표는 저장 요청입니다. 각 씬의 UI 상태는 독립적이고 문서 변경은 공통 저장소가 기준 버전과 함께 확정합니다.","rows":[[{"id":"a","label":"씬 A","detail":["기준 문서 v7 · 편집 X"]},{"id":"b","label":"씬 B","detail":["기준 문서 v7 · 편집 Y"]}],[{"id":"store","label":"공용 문서 저장소","detail":["version 조건 · 충돌 정책","한 문서의 저장 순서"]}],[{"id":"disk","label":"복구 가능한 저장본","detail":["본문과 버전을 함께 보존"]}]],"edges":[{"from":"a","to":"store","label":"기준 v7 저장"},{"from":"b","to":"store","label":"기준 v7 저장"},{"from":"store","to":"disk","label":"조건부 확정"}]}
```

한 씬에서 마지막으로 완료됐다는 이유로 다른 씬의 새 본문을 덮으면 안 됩니다. 요청 시작 순서와 디스크 쓰기 완료 순서는 다를 수 있습니다.

## 동일 버전 중복과 다른 편집 충돌은 다릅니다

SceneDelegate와 SwiftUI scenePhase가 같은 변경 버전 8의 저장을 동시에 요청하면 하나로 합칠 수 있습니다. 그러나 A와 B가 각각 기준 7에서 다른 본문을 만들었다면 “버전 숫자가 같다”는 이유로 동일 저장으로 합쳐서는 안 됩니다. 문서 ID·기준 버전·변경 ID와 내용의 의미를 구분합니다.

```text
commitEdit(documentId, expectedVersion, changeId, newBody):
    in_one_storage_transaction:
        if changeId was already committed: return recorded_result
        current = load_for_update(documentId)
        if current.version != expectedVersion:
            return conflict(current)
        store(body=newBody, version=current.version+1)
        record(changeId, resultVersion=current.version+1)
    return committed_result
```

A가 v8을 만든 뒤 B가 기준 v7로 저장하면 충돌이 됩니다. 독립 필드 변경은 병합할 수 있지만 같은 문단 수정·삭제·권리 변경은 자동 병합이 의미를 망칠 수 있어 사용자 판단이나 도메인 규칙이 필요합니다. 버전만 8로 고쳐 같은 옛 본문을 다시 보내면 보호를 우회합니다.

## 저장은 작은 복구 단위로 만듭니다

변경 시점 저장이나 짧은 debounce는 종료 직전만 저장하는 것보다 유실 범위를 줄입니다. debounce 기간의 손실을 허용할 수 있는지 명시하고 백그라운드로 갈 때 가능한 저장을 마무리합니다. 백그라운드 실행 허용 시간은 무제한 완료 보장이 아닙니다.

큰 파일을 제자리 덮어쓰다 중단되면 기존 복구본까지 손상될 수 있습니다. 새 파일을 완성·검증한 뒤 교체하고 내용 버전과 완료 정보를 일치시킵니다. 원자 교체의 가시성과 전원 장애 내구성은 파일시스템 계약에 맞게 구분합니다. 저장 실패를 UI에 성공으로 표시하지 않고 다음 실행에서 마지막 유효본과 미완료 작업을 복구할 수 있게 합니다.

## 오래된 저장의 완료가 최신 상태를 되돌리지 않게 합니다

v8 저장이 느린 동안 v9가 먼저 끝나고 이후 v8이 고정 파일 경로를 덮으면 데이터가 역행합니다. 문서별 저장을 직렬화하거나 고유 버전 파일을 만든 뒤 권위 포인터를 조건부로 갱신해야 합니다. UI의 savedVersion도 완료 순서가 아니라 실제 확정 버전을 따라야 합니다.

업로드 응답 유실은 로컬 저장 실패와 다릅니다. 원격에는 이미 반영됐을 수 있으므로 작업 ID와 결과 조회를 유지해야 합니다. 앱이 다시 활성화되면 현재 문서와 동기화 상태를 읽고, 재전송·충돌·완료를 구분합니다.

## 복구 시험은 한 씬씩 다르게 움직입니다

두 씬에서 같은 문서를 열고 한쪽만 백그라운드로 보내며 동시에 저장합니다. 같은 changeId 중복은 하나로, 다른 기준 편집은 충돌 또는 의도한 병합으로 처리되어야 합니다. 입력 직후·파일 작성 중·교체 직전·원격 성공 응답 전 중단을 시험합니다.

실제 iOS 백그라운드 API와 문서 저장 프레임워크의 세부 지원은 대상 버전에서 확인해야 합니다. 이 노트의 목적은 특정 콜백 횟수를 암기하는 것이 아니라 어떤 순간에 중단돼도 마지막 유효 상태와 남은 작업을 설명할 수 있게 만드는 것입니다.
