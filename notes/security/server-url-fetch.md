---
id: server-url-fetch
title: 서버 URL 조회의 목적지 검증과 연결 제한
topic: 보안
summary: URL 파싱·DNS·실제 연결·redirect를 하나의 허용 경계로 묶고 TLS 이름·egress·자격 전달·응답 예산을 설명합니다.
questionIds: [security-ssrf-url-fetch]
---

# 서버 URL 조회의 목적지 검증과 연결 제한

## 서버는 사용자 브라우저보다 더 많은 곳에 닿을 수 있습니다

사용자가 넣은 URL을 서버가 대신 읽어 미리보기를 만드는 기능을 생각해 보겠습니다. 서버는 내부 관리 API나 클라우드 메타데이터 서비스에 접근할 수 있을 수 있습니다. 사용자가 직접 읽을 수 없는 위치를 서버 자격과 네트워크로 읽게 되는 것이 SSRF의 핵심입니다.

URL이 문법적으로 유효하고 scheme이 HTTPS여도 그 위치를 서버가 방문해도 된다는 뜻은 아닙니다. 목적지·해석·연결·응답 공개에 각각 제한이 필요합니다.

## 허용 범위를 좁힌 뒤 같은 파서 의미를 유지합니다

가능하면 등록된 몇 도메인만 허용합니다. URL 파서로 scheme·hostname·port·userinfo를 분리하고, 사용하지 않는 사용자 정보·scheme·port를 거절합니다. 문자열에 도메인 이름이 들어 있는지나 접미사 일부가 맞는지만 확인하면 다른 host를 허용할 수 있습니다. 국제 도메인·IP 리터럴·IPv6 등 정규화 계약도 일치시킵니다.

| 경계 | 필요한 검사 | 남는 문제 |
| --- | --- | --- |
| URL 파싱 | 허용 scheme·host·port | DNS의 실제 주소 |
| 이름 해석 | 허용 주소 범위·주소 후보 | 연결 때 재해석 변화 |
| 연결 | 검사한 대상과 실제 peer 결합 | TLS 이름·proxy 경로 |
| redirect | 매 hop 새 목적지 검사 | 쿠키·Authorization 전달 |
| 응답 | 바이트·시간·형식 제한 | 사용자에게 공개할 권한 |

사설 주소 문자열 몇 개만 차단하는 목록은 모든 내부·특수 주소를 포괄한다고 단정할 수 없습니다. 환경에 맞는 네트워크 허용 정책과 검증된 주소 분류를 사용하고, IPv4-mapped IPv6 등 같은 목적지의 다른 표현도 다룹니다.

## DNS 검사와 실제 연결이 다른 주소를 사용하면 안 됩니다

첫 DNS 조회 결과가 공개 주소라 통과시킨 뒤 HTTP 라이브러리가 다시 조회해 내부 주소에 연결하면 검사는 실제 연결을 보호하지 못합니다. 허용한 주소에 실제 연결을 결합하고, 재시도·다중 A/AAAA 후보도 모두 같은 검사를 적용해야 합니다. 선택한 IP로 연결하더라도 TLS 기대 hostname과 SNI는 원래 허용한 서비스 이름을 유지해 올바른 인증서를 검증합니다.

```diagram
{"title":"검사한 목적지와 실제 연결을 묶습니다","caption":"화살표는 한 hop의 처리 순서입니다. redirect가 발생하면 새 URL부터 같은 검사를 반복하고 총 횟수·시간 예산을 줄입니다.","rows":[[{"id":"url","label":"URL 파싱·허용 목록"}],[{"id":"dns","label":"DNS 주소 후보 검사"}],[{"id":"connect","label":"허용 주소에만 연결","detail":["원래 TLS 이름 유지"]}],[{"id":"response","label":"제한된 응답 읽기","detail":["redirect는 처음부터 재검사"]}]],"edges":[{"from":"url","to":"dns","label":"정규화한 host"},{"from":"dns","to":"connect","label":"검사와 사용 결합"},{"from":"connect","to":"response","label":"시간·바이트 상한"}]}
```

프록시를 사용하면 실제 목적지의 이름 해석과 연결이 프록시에서 일어날 수 있습니다. 로컬 IP 확인만으로 프록시의 최종 peer를 증명하지 않습니다. 프록시 자체의 목적지 정책을 강제하거나 서버 조회 경로의 resolver·proxy 구성을 통제합니다. 환경 변수에서 우연히 상속된 proxy나 인증 설정도 조사합니다.

## Redirect마다 자격과 목적지를 다시 판단합니다

허용된 공개 URL이 내부 주소로 redirect할 수 있으므로 자동 따라가기를 끄거나 각 hop을 검사할 수 있는 클라이언트를 사용합니다. 이동 횟수뿐 아니라 전체 deadline·누적 응답 바이트도 제한합니다. 새 목적지에 기존 쿠키·Authorization·클라이언트 인증서를 자동 전달하지 않습니다.

HTTP 오류나 느린 스트림·압축된 큰 응답도 예산 안에서 종료합니다. Content-Length가 없거나 거짓일 수 있으므로 실제 읽은 바이트와 해제 후 크기를 제한합니다. 응답을 앱 origin의 HTML로 다시 내보내면 XSS가 될 수 있어 텍스트 추출·안전한 미리보기·격리 origin 같은 별도 출력 정책을 둡니다.

## 네트워크 경계가 마지막 방어선입니다

조회 worker를 내부 관리자 권한과 클라우드 자격 없이 실행하고 egress에서 허용 목적지만 열어 둡니다. 앱 검사 오류가 생겨도 내부망 접근을 제한하는 독립 방어입니다. 기본 NetworkPolicy가 도메인·HTTP 경로별 정책을 모두 제공한다고 가정하지 않고 CNI·egress proxy의 실제 범위를 확인합니다.

읽을 수 있는 자료라고 모든 사용자에게 공개할 수 있는 것도 아닙니다. URL 접수자의 자격·목적·데이터 공개 범위를 별도로 검사합니다. 외부 서버의 응답 본문에 들어 있는 다음 URL이나 명령을 자동 실행하는 범위 확장도 피합니다.

## 통제된 목적지만 사용해 거절을 확인합니다

자신이 관리하는 테스트 resolver·HTTP 서버로 공개 후보와 금지 후보, redirect, DNS 응답 변화, 다중 주소, 느린 응답을 재현합니다. 검사 로그의 허용 주소와 실제 연결 peer가 일치하는지 확인하고, 차단 결과만 아니라 정상 목적지의 TLS 이름 검증도 유지되는지 봅니다.

실제 내부 메타데이터나 타인의 서비스를 탐색하지 않습니다. 원문 URL의 자격·query 비밀을 로그에 남기지 않고 거절 단계·정규화한 목적지 분류·추적 ID로 진단합니다. 이 노트는 방어 설계이며 실제 서버에서 SSRF 시험을 실행한 보고서가 아닙니다.
